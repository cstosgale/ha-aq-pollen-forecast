services:
  hadev:
    container_name: hadev
    
    ports:
      - 8123:8123/tcp
    volumes:
      - ../..:/workspaces:cached
      - ../../devcontainers/hadev/config:/config
      - ../custom_components:/config/custom_components
